# h2spec cases allowed to fail against the tunnel edge, one per line:
#   <section>/<case>   <one-line justification>
#
# Keep this list as short as possible. A case that starts passing is
# reported by check_h2spec.sh so the entry can be removed.
#
# Nothing allow-listed yet: the edge is hyper's h2 server, which is
# h2spec-clean. Entries here would be for behaviour the *proxy* changes
# (e.g. a 4.3/x HPACK case if we ever re-encode headers).
