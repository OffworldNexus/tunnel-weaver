# Autobahn fuzzingclient cases allowed to be non-OK through the tunnel:
#   <case>   <one-line justification>
#
# Both hops are opaque byte pipes after the 101, so WebSocket behaviour is
# the echo origin's (Python `websockets`), not ours; the tunnel is only
# judged on delivering frames and the close handshake unchanged.
#
# Case selection (fuzzingclient.json): all of §1-7 and §10 — framing,
# fragmentation, control frames, UTF-8, close codes — which is what the
# tunnel actually mediates. §9 (performance/limits) is out. §12/§13
# (permessage-deflate) are sampled (12.1.4-5, 13.1.4-5): every case in
# those sections is ~1000 sequential round-trips, i.e. ~75 s each over a
# real WAN regardless of the implementation, so the full 216 would take
# hours and prove nothing further; the sample still shows compressed
# frames of several sizes survive both hops intact.

7.1.5   origin (Python websockets) answers fragment-then-Close with 1002 instead of 1000; origin's choice, carried unchanged by the tunnel
