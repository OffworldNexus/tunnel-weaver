# Autobahn fuzzingclient cases allowed to be non-OK through the tunnel:
#   <case>   <one-line justification>
#
# After the 101 both hops are opaque byte pipes: WebSocket framing, control
# frames, fragmentation, the close handshake and UTF-8 validation are the
# *echo origin's* (Python `websockets`) and the Autobahn client's behaviour,
# not the tunnel's. The tunnel is only judged on delivering frames and the
# close handshake unchanged.
#
# Case selection (fuzzingclient.json): the tunnel's own responsibility is
# byte-transparent piping, so the suite keeps §1 (framing), §2 (ping/pong),
# §3 (reserved bits), §4 (opcodes), §5 (fragmentation), §7 (close handling)
# and §10 (auto-fragmentation). §6 (UTF-8 validation) and §12/§13
# (permessage-deflate) test endpoint implementations we do not provide; §12/§13
# additionally cannot finish over a WAN (1000 sequential round-trips per case,
# past Autobahn's 240 s per-case timeout), so those sections are not run at
# all and nothing here needs to allow-list them.

7.1.5   origin (Python websockets) answers fragment-then-Close with 1002 instead of 1000; origin's choice, carried unchanged by the tunnel
7.3.2   origin (Python websockets) close-code choice for a reserved-code Close; endpoint behaviour, carried unchanged by the tunnel
