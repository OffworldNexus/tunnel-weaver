# Autobahn fuzzingclient cases allowed to be non-OK through the tunnel:
#   <case>   <one-line justification>
#
# Both hops are opaque byte pipes after the 101, so WebSocket behaviour is
# the echo origin's (Python `websockets`), not ours; the tunnel is only
# judged on delivering frames and the close handshake unchanged.
#
# Case selection (fuzzingclient.json): §9 (performance/limits) and the
# 1000-message deflate cases 12.1.1-3 / 13.1.1-3 are excluded — they do
# 1000 *sequential* round trips and time out at Autobahn's fixed 60 s
# budget purely from WAN latency (≈90 ms × 1000), which says nothing about
# framing. The remaining 12.1.x / 13.1.x (larger payloads, fewer messages)
# still exercise permessage-deflate end to end.

7.1.5   origin (Python websockets) answers fragment-then-Close with 1002 instead of 1000; origin's choice, carried unchanged by the tunnel
